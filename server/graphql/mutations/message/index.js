var addMessage = require('./add').add;
var removeMessage = require('./remove').remove;
var updateMessage = require('./update').update;

module.exports = {
  addMessage,
  removeMessage,
  updateMessage
}