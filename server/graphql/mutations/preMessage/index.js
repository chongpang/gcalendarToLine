var addPreMessage = require('./add').add;
var removePreMessage = require('./remove').remove;
var updatePreMessage = require('./update').update;

module.exports = {
  addPreMessage,
  removePreMessage,
  updatePreMessage
}